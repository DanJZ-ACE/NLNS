# Code and dependency provenance

The workspace initially contained only Git metadata. The supplied reference executable was 31,744 bytes, CLR v4.0.30319, PE32 I386 with ILOnly (AnyCPU; no 32-bit-required flag), using Windows Forms. Assembly references were mscorlib, System, System.Core, System.Drawing, System.Windows.Forms and System.Web.Extensions, version 4.0.0.0. Version metadata was 0.0.0.0. Its metadata and IL exposed manifest, scan, hash, rename and restore behavior, plus an embedded self-test entry point. No source project or build scripts were available. The original compiler invocation cannot be established from metadata alone; the assembly is consistent with a conventional Framework C# compiler build.

Inspection was static/reflection-only. The supplied executable was not launched, patched, or redistributed. New source was independently written to reproduce the observed functional contract, with intentional safety changes documented in ARCHITECTURE.md. The internal inspection dump is excluded from repository deliverables. No decompiled source or original binary is included.

The icon is an original geometric drawing generated by project-owned C# code. It contains no proprietary logo, copied imagery, fonts or downloaded asset files.

Dependencies are exclusively the user's installed Windows/.NET Framework APIs. There are no NuGet packages or third-party source dependencies, and no Windows/.NET runtime DLLs are redistributed. Calling these platform APIs does not require relicensing the new application under a platform license. MIT applies to this project's new source, documentation and assets; it does not relicense NVIDIA libraries, Microsoft components or the supplied reference executable. No dependency compatibility conflict was identified in the delivered source tree.
